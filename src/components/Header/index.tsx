import type { SmartHeaderProps } from "./types";

const SmartHeader: React.FC<{}> = ({}) => {
    return (
        <header>
            Hello From Header
        </header>
    )
}

export default SmartHeader;